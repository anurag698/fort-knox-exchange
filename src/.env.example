# -----------------------------------------------------------------------------
# Firebase Service Account Credentials
#
# For server-side Firebase Admin SDK initialization.
# Provide the full JSON content as a single, compact line (e.g., using `jq -c .`).
# This is the most common method for Vercel, Cloud Run, etc.
# -----------------------------------------------------------------------------
# FIREBASE_SERVICE_ACCOUNT_JSON=""

# --- OR ---

# If your environment supports Application Default Credentials (ADC), like some
# Google Cloud runtimes, you can point to a file path.
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account.json"

# -----------------------------------------------------------------------------
# Blockchain HD Wallet Extended Public Keys (XPUBS)
#
# These are PUBLIC keys used for generating deposit addresses.
# It is safe to store these in environment variables.
# -----------------------------------------------------------------------------
# Bitcoin (BTC) xpub (can be xpub, ypub, or zpub depending on your wallet format)
XPUB_BTC="your_bitcoin_xpub_here"

# Ethereum (ETH) and ERC20 tokens (USDT, USDC, etc.) xpub
# This should be derived from a path like m/44'/60'/0'/0
ETH_XPUB="your_ethereum_xpub_here"


# -----------------------------------------------------------------------------
# Blockchain RPC Endpoints
#
# URLs for connecting to blockchain nodes.
# Use services like Infura, Alchemy, or your own node.
# -----------------------------------------------------------------------------
# Ethereum Mainnet RPC URL
ETH_RPC_URL="https://mainnet.infura.io/v3/your-infura-project-id"

# Polygon (Matic) Mainnet RPC URL
POLYGON_RPC_URL="https://polygon-mainnet.infura.io/v3/your-infura-project-id"

# Binance Smart Chain (BSC) Mainnet RPC URL
BSC_RPC_URL="https://bsc-dataseed.binance.org/"


# -----------------------------------------------------------------------------
# Third-Party API Keys
#
# Keys for external services like 1inch for DEX aggregation.
# -----------------------------------------------------------------------------
ONEINCH_API_KEY="your_1inch_api_key_here"


# -----------------------------------------------------------------------------
# Hot Wallet Configuration
#
# !! WARNING: FOR PRODUCTION, NEVER STORE A RAW PRIVATE KEY HERE !!
# Use a secure secret manager (like Google Secret Manager, AWS KMS, HashiCorp Vault)
# and have your withdrawal service fetch the key at runtime.
#
# For local development and testing ONLY, you can use a raw private key.
# This key is for the wallet that will execute on-chain DEX swaps and withdrawals.
# -----------------------------------------------------------------------------
# For local development only
HOT_WALLET_PRIVATE_KEY="your_hot_wallet_private_key_for_testing"

# In production, you would use a reference to a secret, e.g.:
# GCLOUD_SECRET_NAME_FOR_HOT_WALLET="projects/your-gcp-project/secrets/hot-wallet-key/versions/latest"
