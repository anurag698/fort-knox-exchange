
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // HELPER FUNCTIONS
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isAdmin() {
      // In production, this should rely on a custom claim set securely by a backend process.
      // return request.auth.token.admin == true;
      // For this project, we check a field in the user's document.
      return isSignedIn() && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'ADMIN';
    }

    // USER DATA
    match /users/{userId} {
      // Users can read their own profile. Admins can read any profile.
      allow get: if isOwner(userId) || isAdmin();
      // Users can update their own profile.
      allow update: if isOwner(userId);
      // Only authenticated users can create their own user document.
      allow create: if isSignedIn();
      // Admins can list all users.
      allow list: if isAdmin();
      
      // Balances can only be read by the owner. ALL writes must be from the backend.
      match /balances/{tokenId} {
          allow get: if isOwner(userId);
          allow list: if isOwner(userId);
          allow write: if false;
      }
    }

    // ORDERS & TRADES
    match /orders/{orderId} {
        // Users can create their own orders.
        allow create: if isSignedIn() && request.resource.data.userId == request.auth.uid;
        // Users can read their own orders. Admins can read any.
        allow get: if (isSignedIn() && resource.data.userId == request.auth.uid) || isAdmin();
        // Users can list their own orders.
        allow list: if isSignedIn() && request.query.where.size() > 0 && request.query.where[0][0] == 'userId' && request.query.where[0][2] == request.auth.uid;
        // Users can cancel their own open orders.
        allow update: if isSignedIn() 
                      && resource.data.userId == request.auth.uid
                      && request.resource.data.status == 'CANCELED';
        allow delete: if false;

        // Trades are part of an order, readable by the order owner or an admin.
        match /trades/{tradeId} {
            allow get: if (isSignedIn() && get(/databases/$(database)/documents/orders/$(orderId)).data.userId == request.auth.uid) || isAdmin();
            allow list: if (isSignedIn() && get(/databases/$(database)/documents/orders/$(orderId)).data.userId == request.auth.uid) || isAdmin();
            allow write: if false; // Server-only
        }
    }

    // PUBLIC DATA (Tokens, Markets, Market Data)
    match /tokens/{tokenId} {
      allow get, list: if true;
      allow write: if false; // Server-only
    }
    match /markets/{marketId} {
      allow get, list: if true;
      allow write: if false; // Server-only
    }
     match /market_data/{marketId} {
      allow get, list: if true;
      allow write: if false; // Server-only
    }

    // SERVER-ONLY COLLECTIONS
    // These collections should never be accessed directly by a client.
    match /ledger_entries/{entryId} {
      allow read, write: if false;
    }
    match /dex_transactions/{dexTxId} {
      allow read, write: if false;
    }
    match /idempotency/{key} {
      allow read, write: if false;
    }
    
    // LEGACY COLLECTIONS (to be removed or migrated)
    // Deny access to old data structures to enforce new schema.
    match /assets/{assetId} { allow read, write: if false; }
    match /trades/{tradeId} { allow read, write: if false; }
    match /publicData/{docId} { allow read, write: if false; }
    match /adminUsers/{adminId} { allow read, write: if false; }
    match /{path=**}/sessions/{sessionId} { allow read, write: if false; }
    match /{path=**}/deposits/{depositId} { allow read, write: if false; }
    match /{path=**}/withdrawals/{withdrawalId} { allow read, write: if false; }
    match /{path=**}/ledgerEntries/{ledgerEntryId} { allow read, write: if false; }
  }
}
