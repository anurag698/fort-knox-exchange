
# -----------------------------------------------------------------------------
# REQUIRED - Firebase Admin SDK Credentials (Server-Side)
# -----------------------------------------------------------------------------
# For production environments (Cloud Run, Vercel), it's recommended to set this 
# as a secret environment variable. It should be the full JSON content of your
# service account key, as a single line string.
# Example: FIREBASE_SERVICE_ACCOUNT_JSON='{"type": "service_account", ...}'
FIREBASE_SERVICE_ACCOUNT_JSON=

# Alternatively, for environments that support file paths (like local dev or some VMs),
# you can provide the path to your service account key file. 
# The `firebase-admin.ts` loader will prefer FIREBASE_SERVICE_ACCOUNT_JSON if both are set.
# GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/service-account.json


# -----------------------------------------------------------------------------
# REQUIRED - Blockchain Address Derivation (Server-Side)
# -----------------------------------------------------------------------------
# These are the Extended Public Keys (xPubs) for your wallets. 
# These are PUBLIC keys and are safe to store as environment variables on your server.
# DO NOT store private keys or mnemonic phrases here.

# Ethereum (and ERC20 tokens like USDT, AAVE, etc.)
ETH_XPUB=
# Bitcoin
BTC_XPUB=


# -----------------------------------------------------------------------------
# OPTIONAL - RPC Endpoints & Chain Configuration (Server-Side)
# -----------------------------------------------------------------------------
# Your app needs to connect to blockchain nodes to broadcast transactions or query data.
# It's highly recommended to use a dedicated RPC provider like Alchemy, Infura, or QuickNode.

# Ethereum Mainnet RPC URL
ETH_NETWORK_RPC=

# Polygon Mainnet RPC URL
POLYGON_NETWORK_RPC=https://polygon-rpc.com


# -----------------------------------------------------------------------------
# OPTIONAL - Hot Wallet Configuration (Server-Side)
# -----------------------------------------------------------------------------
# This private key is for a "hot wallet" used for processing DEX swaps.
# In production, this key should be managed by a secure secret manager (e.g., Google Secret Manager, AWS KMS).
# For local development, you can set it here, but DO NOT commit it.
HOT_WALLET_PRIVATE_KEY=


# -----------------------------------------------------------------------------
# OPTIONAL - Third-Party API Keys (Server-Side)
# -----------------------------------------------------------------------------
# API Key for the 1inch DEX aggregator service.
ONEINCH_API_KEY=


# -----------------------------------------------------------------------------
# NEXT.JS PUBLIC VARIABLES
# -----------------------------------------------------------------------------
# These variables are exposed to the client-side and should not contain secrets.
# Prefix with NEXT_PUBLIC_

# The base URL of your application. Useful for constructing absolute URLs for API calls.
NEXT_PUBLIC_BASE_URL=http://localhost:9002
