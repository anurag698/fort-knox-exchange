# This is an example file. Copy it to .env and fill in your actual values.
# DO NOT COMMIT .env TO VERSION CONTROL.

# Firebase Service Account JSON (as a single, compact line)
# Used by the Firebase Admin SDK on the server.
# Generate from your Firebase project settings.
# Example command to format: export FIREBASE_SERVICE_ACCOUNT_JSON="$(jq -c . /path/to/your/service-account.json)"
FIREBASE_SERVICE_ACCOUNT_JSON=""

# Blockchain HD Wallet Extended Public Keys (XPUBs)
# Used for generating deposit addresses without exposing private keys.
# These are public keys, but should still be treated as sensitive configuration.
ETH_XPUB="xpub..."
BTC_XPUB="xpub..."
# Add other chain xpubs as needed (e.g., TRON_XPUB, SOL_XPUB)

# Blockchain RPC Endpoints
# Required for interacting with blockchain networks (e.g., checking balances, broadcasting transactions).
# Get these from services like Alchemy, Infura, or QuickNode.
ETH_NETWORK_RPC="https://eth-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY"
POLYGON_NETWORK_RPC="https://polygon-mainnet.g.alchemy.com/v2/YOUR_ALCHEMY_KEY"
# Add other chain RPCs as needed

# Hot Wallet Private Key
# DANGER: For development and testing only. In production, use a KMS or HSM.
# This key is used by the wallet-service.ts to sign and broadcast transactions.
HOT_WALLET_PRIVATE_KEY="0x..."

# Third-Party API Keys
# For services like 1inch for DEX aggregation.
ONEINCH_API_KEY="your_1inch_api_key_here"

# Derivation Paths (for reference, can be hardcoded if consistent)
ETH_DERIVATION_PATH="m/44'/60'/0'/0"
BTC_DERIVATION_PATH="m/84'/0'/0'/0" # bech32

# Chain IDs (for reference)
ETH_CHAIN_ID=1
POLYGON_CHAIN_ID=137
BSC_CHAIN_ID=56

# Address Generation Configuration
ETH_ADDRESS_GAP_LIMIT=20
BTC_ADDRESS_GAP_LIMIT=20
